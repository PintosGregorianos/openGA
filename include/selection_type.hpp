#pragma once

namespace selection_types {
    namespace roulette {
        enum {
            linearFitness,expFitness,linearScore,expScore
        };
    };
};

using selection_type = unsigned short int;
